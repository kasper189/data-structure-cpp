file(GLOB SRCS *.cpp)

ADD_EXECUTABLE(LINKED_LIST_TEST ${SRCS})

TARGET_LINK_LIBRARIES(
    LINKED_LIST_TEST
    LINKED_LIST
    libgtest
)

add_test(NAME LINKED_LIST_TEST
         COMMAND LINKED_LIST_TEST)